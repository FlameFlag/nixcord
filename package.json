{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@nixcord/parser": "^1.0.0",
    "@nixcord/shared": "^1.0.0"
  },
  "scripts": {
    "build": "bun run --filter '*' build && tsc --build --emitDeclarationOnly --force",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build",
    "clean": "rm -rf packages/*/dist",
    "format": "biome format --write packages/*/src packages/*/tests",
    "format:check": "biome format packages/*/src packages/*/tests",
    "lint": "fallow dead-code && fallow dupes",
    "lint:health": "fallow health",
    "smoke:built-cli": "bun run build && bun scripts/smoke-built-cli.ts",
    "smoke:upstream": "bun run build && bun scripts/smoke-upstream.ts",
    "check": "bun run typecheck && bun run lint && bun run format:check && bun run smoke:built-cli"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.6.2",
    "@vitest/coverage-v8": "~4.1.5",
    "@vitest/ui": "~4.1.5",
    "fallow": "^2.69.0",
    "typescript": "^6.0.3",
    "vite": "~8.0.11",
    "vitest": "~4.1.5"
  }
}
